<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_it_ticket_form" model="ir.ui.view">
        <field name="name">it.ticket.form</field>
        <field name="model">it.ticket</field>
        <field name="arch" type="xml">
            <form string="IT Ticket">
                <header>

                    <button name="action_submit"
                            string="Submit"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'draft'"/>

                    <button name="action_manager_approve"
                            string="Approve"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'manager_approval'"/>

                    <button name="action_reject"
                            string="Reject"
                            type="object"
                            invisible="state not in ['manager_approval','it_approval']"/>

                    <button name="action_it_approve"
                            string="IT Approve"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'it_approval'"/>

                    <button name="action_start_work"
                            string="Start Work"
                            type="object"
                            invisible="state != 'assigned'"/>

                    <button name="action_done"
                            string="Mark Done"
                            type="object"
                            class="oe_highlight"
                            invisible="state != 'in_progress'"/>

                    <field name="state"
                           widget="statusbar"
                           statusbar_visible="draft,manager_approval,it_approval,assigned,in_progress,done,rejected"/>

                </header>

                <sheet>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="ticket_type"/>
                            <field name="priority" widget="priority"/>
                        </group>
                        <group>
                            <field name="subject"/>
                            <field name="required_date"/>
                        </group>
                    </group>

                    <group>
                        <field name="description"/>
                    </group>
                </sheet>

                <div class="oe_chatter">
                    <field name="message_ids"/>
                </div>

            </form>
        </field>
    </record>

</odoo>
